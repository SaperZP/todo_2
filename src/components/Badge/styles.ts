import { SystemStyleObject } from '@mui/system';

export const badgeStyles = {
  wrapper: {
    display: 'flex',
    alignItems: 'center',
    gap: '5px',
    padding: '4px 8px',
    borderRadius: '4px',
  } as SystemStyleObject,
};
